pipeline {
    agent any
    
    stages() {
        stage('git clone') {
            steps() {
                git 'https://github.com/mystic707/GestureControlSample.git'
                git branch: "feature/jenkinsfile_test"
            }
        }
        
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }  
    }
}